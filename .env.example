# TradeShield Agent — environment variables (copy to .env and fill in)
# Everything is OPTIONAL: with no keys, the AI valuation agent runs a
# deterministic fallback (mock data calibrated to June 2026) so the demo
# always works. Add keys to use a real LLM + live data sources.

# ---------------------------------------------------------------------------
# 1) LLM provider for tool calling (pick ONE). OpenAI-compatible /chat/completions.
# ---------------------------------------------------------------------------
# DeepSeek — https://platform.deepseek.com  (model: deepseek-chat)
DEEPSEEK_API_KEY=

# Qwen / 通义千问 via Alibaba DashScope (OpenAI-compatible mode)
# https://dashscope.console.aliyun.com  (model: qwen-plus / qwen-max)
DASHSCOPE_API_KEY=
# DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Tencent Cloud TokenHub / Hunyuan (OpenAI-compatible). Model is LOCKED to
# hy3-preview (the only model permitted on this account). When set, this provider
# wins by default and the demo narrator (npm run demo) uses it with auto-fallback.
Tencent_API_KEY=
Tencent_BASE_URL=https://tokenhub.tencentmaas.com/v1

# Optional explicit selection / overrides:
# LLM_PROVIDER=tencent             # tencent | deepseek | qwen | openai
# LLM_MODEL=deepseek-chat          # ignored for Tencent (locked to hy3-preview)
# Fully custom OpenAI-compatible endpoint:
# LLM_BASE_URL=https://your-endpoint/v1
# LLM_API_KEY=...

# ---------------------------------------------------------------------------
# 2) Live commodity price (copper, USD/MT). Pick one; else mock.
# ---------------------------------------------------------------------------
# Alpha Vantage (free) — COPPER global monthly price. https://www.alphavantage.co/support/#api-key
ALPHAVANTAGE_API_KEY=
# metalpriceapi.com (freemium) — LME copper real-time. https://metalpriceapi.com
METALPRICE_API_KEY=
# COMMODITY_PRICE_PROVIDER=alphavantage   # metalprice | alphavantage | mock

# Regional physical premium (CIF Shanghai / Yangshan), USD/MT.
# No free API (SMM/Fastmarkets are paid); set the current value manually.
# REGION_PREMIUM_USD_PER_MT=70

# ---------------------------------------------------------------------------
# 3) Historical comparable trade prices (UN Comtrade, by HS code). Free.
# ---------------------------------------------------------------------------
# Register at https://comtradedeveloper.un.org -> subscribe -> "primary key".
COMTRADE_PRIMARY_KEY=

# ---------------------------------------------------------------------------
# 4) xAPI (xapi.to) — live real-world risk signals for the world-risk agent.
#    One key calls Twitter/X, Google News and prediction markets; output is JSON.
#    Used by `npm run intel` and POST /api/intel/world-risk. With NO key, the
#    world-risk agent runs on deterministic offline fixtures (demo always works).
#    Get a key: `npx xapi-to register` (invite code: xapito), then
#    `npx xapi-to oauth bind --provider twitter`. See docs/xapi-integration.md.
# ---------------------------------------------------------------------------
XAPI_KEY=
# XAPI_ACTION_HOST=action.xapi.to   # override the action gateway host (rarely needed)

# Unified runtime mode. Demo is the safe default; Live never silently falls back to mock tx.
# DEMO_MODE=true

# x402-protected premium risk intel (optional; free endpoints still work).
# Used by `npm run smoke:x402` and the AI Agent's paid-intel flow.
# x402 network: CAIP-2 identifier (eip155:10143 for Monad, eip155:1439 for Injective).
# X402_NETWORK=eip155:1439
# Paid report cache/envelope TTL in seconds (10-86400; default 300).
# X402_REPORT_TTL_SECONDS=300
# X402-15 testnet-only live smoke (explicit one-run consent is mandatory)
# X402_LIVE_CONFIRM=injective-testnet
# X402_BUYER_PRIVATE_KEY=0x...
# X402_ALLOW_SELF_PAYMENT=false
# X402_LIVE_AMOUNT_ATOMIC=1000
# X402_RECOVER_PAYMENT_TX=0x... # resume after a confirmed payment without paying again
# x402 facilitator URL — settlement relay.
# X402_FACILITATOR_URL=https://x402-facilitator.molandak.org
# USDC token address for x402 settlement.
# X402_USDC_ADDRESS=
# White Agent wallet private key — signs x402 EIP-3009 payments.
# WHITE_AGENT_PRIVATE_KEY=
# Address that receives x402 USDC payments.
# X402_PAY_TO_ADDRESS=

Ali_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
Ali_API_KEY=
# 使用模型 qwen3.7-max,python 可以使用 conda activate 一个新环境
# NOTE: .env.example must never contain real keys — keep all *_API_KEY values blank.
