{
  "name": "tradeshield-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Hardhat contracts for TradeShield eBL-backed RWA offering (WEB3-6 ~ WEB3-9).",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:tradeshield:sepolia": "hardhat run scripts/deploy-tradeshield.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "chai": "^4.5.0",
    "dotenv": "^16.4.7",
    "ethers": "^6.13.4",
    "hardhat": "^2.22.17"
  }
}
